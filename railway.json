{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "echo 'Running database schema sync...' && npm run db:push && echo 'Running additional migrations...' && node scripts/migrate.js && echo 'Schema sync complete. Starting server...' && npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
